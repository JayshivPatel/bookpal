import * as React from 'react';
import { ScrollView, View } from 'react-native';



const Learn = () => {
    return (
        <View style={{flex: 1}}>
            <ScrollView>
            </ScrollView>
        </View>
    );
};


export default Learn;